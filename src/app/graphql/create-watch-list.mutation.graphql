mutation createWatchList($accountId: Int!, $eventId: String!) {
  createWatchList(
    input: {
      watchList: {
        accountId: $accountId,
        eventId: $eventId
      }
    }
  ) {
    watchList {
      id
    }
  }
}